name: Cypress Tests

on:
  push:
    branches: [ main ]
  pull_request:

jobs:
  cypress-run:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm ci

      - name: Run Cypress tests and generate report
        run: |
          rm -rf reports || true
          npm run test:full

      - name: Upload HTML report
        uses: actions/upload-artifact@v4
        with:
          name: mochawesome-report
          path: reports/html # 🔹 Corrigido: Agora aponta para a pasta real na raiz!

      - name: Upload Cypress videos
        uses: actions/upload-artifact@v4
        with:
          name: cypress-videos
          path: cypress/videos