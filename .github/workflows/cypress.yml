name: Cypress Tests

on:
  push:
    branches: [ main ]
  pull_request:

# 🔹 Força todas as ações do GitHub a usarem o Node 24 e elimina o aviso de depreciação
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  cypress-run:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'

      - name: Install dependencies
        run: npm ci

      - name: Run Cypress tests and generate report
        run: |
          rm -rf cypress/reports || true
          npm run test:full

      - name: Upload HTML report
        uses: actions/upload-artifact@v4
        with:
          name: mochawesome-report
          path: cypress/reports/html

      - name: Upload Cypress videos
        uses: actions/upload-artifact@v4
        with:
          name: cypress-videos
          path: cypress/videos